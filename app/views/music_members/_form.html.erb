<%= form_for(@music_member) do |f| %>
  <% if @music_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@music_member.errors.count, "error") %> prohibited this music_member from being saved:</h2>

      <ul>
      <% @music_member.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
